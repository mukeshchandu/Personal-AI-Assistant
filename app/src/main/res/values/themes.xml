<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Naomi" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Assistant overlay: translucent window so the app underneath stays visible,
         dimmed a little so Naomi's bottom panel is readable on top of it. -->
    <style name="Theme.Naomi.Overlay" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.4</item>
    </style>
</resources>
